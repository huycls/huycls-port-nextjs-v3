import HeroSection from "../../organisms/HeroSection";

const Home = () => {
  return (
    <section
      id="home"
      className="w-full h-full">
      <HeroSection />
    </section>
  );
};

export default Home;
